function XO(str) {
    //code here
  var xCount = 0;
  var oCount = 0;
  str = str.toLowerCase();
  for(var i=0; i<str.length; i++) {
    if(str[i] == "x"){
      xCount++;
    }
    else if(str[i] == "o"){
      oCount++;
    }
  }
  return xCount == oCount;
}
